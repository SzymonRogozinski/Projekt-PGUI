export default class BuyerReview {
  id;
  username;
  type;
  comment;
  constructor({ id, username, type, comment }) {
    this.id = id;
    this.username = username;
    this.type = type;
    this.comment;
  }
}
